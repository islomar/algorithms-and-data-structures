[project]
name = "algorithms-in-python"
version = "0.1.0"
description = "Playground for learning about algorithms and data structures"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pytest>=8.0.0",
    "ruff>=0.12.11",
]
